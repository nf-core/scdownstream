nextflow_workflow {

    name "Test Workflow COMBINE"
    script "subworkflows/local/combine/main.nf"
    workflow "COMBINE"

    tag "subworkflows"
    tag "subworkflows_local"

    test("Should run without failures - without base - stub") {

        options '-stub'

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0]  = channel.of(
                    [[id: 'test1'], file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)],
                    [[id: 'test2'], file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)]
                )
                input[1]  = channel.value([[], []])
                input[2]  = false
                input[3]  = 'deviance'
                input[4]  = 0
                input[5]  = 'scvi'
                input[6]  = null
                input[7]  = ''
                input[8]  = ''
                input[9]  = null
                input[10] = null
                input[11] = ''
                input[12] = ''
                input[13] = 'https://zenodo.org/records/10685499/files/model_v1.1.tar.gz'
                input[14] = null
                input[15] = null
                input[16] = 'condition'
                input[17] = false
                input[18] = 0
                input[19] = 'stratified_label_batch'
                input[20] = 42
                input[21] = 'fast'
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

    test("Should run without failures - without base") {

        when {
            params {
                outdir = "$outputDir"
            }
            workflow {
                """
                input[0] = channel.of(
                    [[id: 'test1'], file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)],
                    [[id: 'test2'], file(params.pipelines_testdata_base_path + '/anndata-variations/batch_correct_name.h5ad', checkIfExists: true)]
                )
                input[1]  = channel.value([[], []])
                input[2]  = false
                input[3]  = 'deviance'
                input[4]  = 0
                input[5]  = 'scvi'
                input[6]  = null
                input[7]  = ''
                input[8]  = ''
                input[9]  = null
                input[10] = null
                input[11] = ''
                input[12] = ''
                input[13] = 'https://zenodo.org/records/10685499/files/model_v1.1.tar.gz'
                input[14] = null
                input[15] = null
                input[16] = 'condition'
                input[17] = false
                input[18] = 0
                input[19] = 'stratified_label_batch'
                input[20] = 42
                input[21] = 'fast'
                """
            }
        }

        then {
            def adata = anndata(workflow.out.h5ad[0][1])
            assert workflow.success
            assert "batch" in adata.obs.colnames
            assert snapshot(
                workflow.out.versions.collect { path(it).yaml },
                adata.yaml
            ).match()
        }

    }

}
